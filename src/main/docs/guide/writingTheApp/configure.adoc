In this sample application, we cache news headlines. Configure your caches size in `application.yml`:

[source, yml]
.src/main/resources/application.yml
----
include::{sourceDir}/src/main/resources/application.yml[tag=config]
----

<1> Configure a cache called "headlines".

TIP: By default Micronaut's cache uses, by default, https://github.com/ben-manes/caffeine[Caffeine].
